fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.4
httpx==0.28.1
python-multipart==0.0.20
sqlalchemy==2.0.36
# Optional. Install only for a Postgres/Redis deployment; the app falls back to
# SQLite and an in-process cache when these are absent.
psycopg[binary]==3.2.3 ; python_version >= "3.10"
redis==5.2.1
