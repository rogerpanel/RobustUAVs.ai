# Research plan & schedule (24 Jul → 28 Sept 2026)

Markdown companion to the LaTeX source (`PLAN_source.tex`). See CLAUDE.md
"Roadmap" for the week-by-week milestones mapped to repo state. Hard constraints:
visa submission 19–20 Aug; Finland/ESTC trip 6–12 Sept (W7 mostly travel).
Heavy technical work is front-loaded W1–W6; W8–W9 are writing, final experiments,
polish. Fallback Path B: Option C alone to the artifact track if the composition
theorem is not tight by W6.

Expected results the paper will claim:
1. Composition theorem — the first formal θ↦δ↦MCR guarantee chaining a network
   detector to a navigation certificate.
2. Headline figure — certified MCR vs detector threshold θ, holding the DO-326A
   floor (≥0.90) across a J/S range where neither layer alone does.
3. Dominance — the composed guarantee strictly dominates autonomy-only and
   network-only baselines.
4. Option C artifact — the open end-to-end benchmark + harness (artifact track).
5. Statistical rigor — multi-seed; Wilcoxon signed-rank + Holm on all headline deltas.
